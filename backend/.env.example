DATABASE_URL=postgres://your-username@localhost:5432/style_engine
JWT_SECRET=change-me-to-secure-secret-key-min-12-chars
PORT=4000
NODE_ENV=development

# Note for macOS users with Homebrew PostgreSQL:
# - Replace 'your-username' with your macOS username (run 'whoami' to find it)
# - No password is required for local development
# - Make sure PostgreSQL is running: brew services start postgresql@16
# - Create the database: /opt/homebrew/opt/postgresql@16/bin/createdb style_engine
