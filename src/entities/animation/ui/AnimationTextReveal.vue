<template>
  <div class="text-reveal">
    <div class="text-reveal__content">
      <span>CSS Animations</span>
    </div>
  </div>
</template>

<style lang="scss" scoped>
.text-reveal {
  display: inline-flex;
  padding: $space-lg $space-2xl;
  border-radius: $border-radius-xl;
  background: linear-gradient(145deg, color-var-alpha('color-bg-primary', 0.94), color-var-alpha('color-accent', 0.08));
  border: 1px solid color-var-alpha('panel-border', 0.24);
  overflow: hidden;
}

.text-reveal__content {
  position: relative;
  overflow: hidden;
}

.text-reveal__content span {
  display: inline-block;
  font-size: $font-size-xl;
  font-weight: $font-weight-bold;
  background: linear-gradient(90deg, $color-primary, $color-accent, $color-primary);
  background-size: 200% auto;
  -webkit-background-clip: text;
  background-clip: text;
  -webkit-text-fill-color: transparent;
  animation: textShine 3s linear infinite;
}

@keyframes textShine {
  to {
    background-position: 200% center;
  }
}
</style>
